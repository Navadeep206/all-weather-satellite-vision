dataset:
  version: v1
  seed: 42

split:
  train_ratio: 0.70
  val_ratio: 0.15
  test_ratio: 0.15
  grouping_key: scene_id

patch:
  size: 256
  overlap: 0
  minimum_valid_fraction: 0.90

generation:
  stage1_variants_per_scene: 2
  stage2_variants_per_scene: 2
  max_scenes: 3
